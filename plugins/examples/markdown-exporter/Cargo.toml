[package]
name = "markdown-exporter"
version = "0.1.0"
edition = "2021"
description = "OxideSEO native plugin: Markdown report exporter"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
